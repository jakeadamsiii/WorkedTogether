import React, {PropTypes} from 'react';
import {Link} from 'react-router';

const Header = () =>{
    return (
        <nav>
            <h1>Witty App Name</h1>
        </nav>
    );
};

export default Header;